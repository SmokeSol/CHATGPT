import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';
import { SOCIAL_CONFIG, socializeContribution } from './social_engine.ts';

const CORS={
  'access-control-allow-origin':'*',
  'access-control-allow-headers':'authorization, x-client-info, apikey, content-type',
  'access-control-allow-methods':'GET, POST, OPTIONS'
};
const json=(body,status=200,cache='no-store')=>new Response(JSON.stringify(body),{status,headers:{...CORS,'content-type':'application/json; charset=utf-8','cache-control':cache}});
const supabase=createClient(Deno.env.get('SUPABASE_URL'),Deno.env.get('SUPABASE_SERVICE_ROLE_KEY'),{auth:{persistSession:false}});
const BUCKET='asv2-payloads-v1';
const POLICY_VERSION='ATLAS_ABUSE_V1_FROZEN';
const FACTORS=['prior_vote_inertia','turnout_habit','personal_economic_conditions','employment_and_income','social_protection_and_public_services','policy_program_fit','governance_and_institutions','territorial_rural_fit','government_reward_punishment','local_candidate_context','other_verified_context'];
const REASONS=new Set(['PRIOR_VOTE_INERTIA','PRIOR_ABSTENTION_INERTIA','TURNOUT_HABIT','ECONOMIC_SELF_INTEREST','EMPLOYMENT_INCOME_FIT','SOCIAL_PROTECTION_PUBLIC_SERVICES_FIT','POLICY_PROGRAM_FIT','GOVERNANCE_INSTITUTIONAL_FIT','TERRITORIAL_RURAL_FIT','GOVERNMENT_REWARD','GOVERNMENT_PUNISHMENT','LOCAL_CANDIDATE_STRENGTH','LOCAL_CANDIDATE_WEAKNESS','ATTITUDE_POSTERIOR','OTHER_VERIFIED_CONTEXT','NO_DIRECTIONAL_EVIDENCE']);
const ROW_KEYS=['anonymous_election_id','anonymous_territory_id','condition_id','batch_id','weighted_archetype_id','turnout_probability','conditional_party_probabilities','factor_importance','reason_codes'].sort();
const enc=new TextEncoder();
function hex(bytes){return Array.from(bytes).map(x=>x.toString(16).padStart(2,'0')).join('')}
async function sha256Text(s){return hex(new Uint8Array(await crypto.subtle.digest('SHA-256',enc.encode(String(s)))))}
async function sha256Json(v){return sha256Text(JSON.stringify(v))}
function randomToken(){const b=new Uint8Array(32);crypto.getRandomValues(b);return btoa(String.fromCharCode(...b)).replace(/\+/g,'-').replace(/\//g,'_').replace(/=+$/,'')}
function clientIp(req){return (req.headers.get('cf-connecting-ip')||req.headers.get('x-forwarded-for')?.split(',')[0]||req.headers.get('x-real-ip')||'0.0.0.0').trim()}
function sameKeys(obj,keys){return obj&&typeof obj==='object'&&!Array.isArray(obj)&&JSON.stringify(Object.keys(obj).sort())===JSON.stringify([...keys].sort())}
function round6(x){return Math.round(Number(x)*1e6)/1e6}
async function storageReady(){const {data,error}=await supabase.storage.from(BUCKET).download('_READY.json');return !error&&!!data}
async function readText(path){const {data,error}=await supabase.storage.from(BUCKET).download(path);if(error||!data)throw new Error('PAYLOAD_NOT_READY');return await data.text()}
async function readJson(path){return JSON.parse(await readText(path))}
async function securityConfig(){const {data,error}=await supabase.from('asv2_security_config').select('*').eq('config_id','PUBLIC_V1').single();if(error||!data)throw new Error('SECURITY_CONFIG_MISSING');const siteKey=Deno.env.get('TURNSTILE_SITE_KEY')||data.turnstile_site_key||'';const secret=Deno.env.get('TURNSTILE_SECRET_KEY')||'';return {...data,turnstile_site_key:siteKey,turnstile_secret_present:!!secret,turnstile_ready:(!data.turnstile_required)||!!(siteKey&&secret),turnstile_secret:secret}}
async function pseudonyms(req,browserId,cfg){if(typeof browserId!=='string'||browserId.length<16||browserId.length>256)throw new Error('BROWSER_ID_INVALID');const ip=clientIp(req);return {browser_hash:await sha256Text(cfg.ip_pepper+'|browser|'+browserId),ip_hash:await sha256Text(cfg.ip_pepper+'|ip|'+ip)}}
async function verifyTurnstile(req,token,cfg){if(!cfg.turnstile_required)return{success:true,disabled:true};if(!cfg.turnstile_ready)throw new Error('TURNSTILE_NOT_CONFIGURED');if(typeof token!=='string'||token.length<10)throw new Error('TURNSTILE_TOKEN_MISSING');const form=new FormData();form.append('secret',cfg.turnstile_secret);form.append('response',token);form.append('remoteip',clientIp(req));const r=await fetch('https://challenges.cloudflare.com/turnstile/v0/siteverify',{method:'POST',body:form});const out=await r.json();if(!out?.success)throw new Error('TURNSTILE_FAILED');if(out.action&&out.action!=='atlas_claim')throw new Error('TURNSTILE_ACTION_MISMATCH');return out}
function validateRows(rows,w,voterBatch){if(!Array.isArray(rows)||rows.length!==32)return{ok:false,error:'ROW_COUNT'};const voters=voterBatch?.voter_archetypes||[],partyIds=[...(voterBatch?.available_party_ids||[])].sort();if(voters.length!==32||partyIds.length!==9)return{ok:false,error:'VOTER_BATCH_INVALID'};const seen=new Set();for(let i=0;i<32;i++){const r=rows[i],v=voters[i];if(!sameKeys(r,ROW_KEYS))return{ok:false,error:'ROW_SHAPE',row:i};if(r.anonymous_election_id!==w.anonymous_election_id||r.anonymous_territory_id!==w.anonymous_territory_id||r.condition_id!==w.condition_id||r.batch_id!==w.batch_id)return{ok:false,error:'WORK_IDENTITY',row:i};if(String(r.weighted_archetype_id)!==String(v.weighted_archetype_id))return{ok:false,error:'ARCHETYPE_ORDER',row:i};if(seen.has(r.weighted_archetype_id))return{ok:false,error:'DUPLICATE_ARCHETYPE',row:i};seen.add(r.weighted_archetype_id);const u=Number(r.turnout_probability);if(!Number.isFinite(u)||u<0||u>1)return{ok:false,error:'TURNOUT',row:i};if(!sameKeys(r.conditional_party_probabilities,partyIds))return{ok:false,error:'PARTY_KEYS',row:i};const p=partyIds.map(k=>Number(r.conditional_party_probabilities[k]));if(p.some(x=>!Number.isFinite(x)||x<0||x>1)||Math.abs(p.reduce((a,b)=>a+b,0)-1)>1e-9)return{ok:false,error:'PARTY_SIMPLEX',row:i};if(!sameKeys(r.factor_importance,FACTORS))return{ok:false,error:'FACTOR_KEYS',row:i};const f=FACTORS.map(k=>Number(r.factor_importance[k]));if(f.some(x=>!Number.isFinite(x)||x<0||x>1)||Math.abs(f.reduce((a,b)=>a+b,0)-1)>1e-9)return{ok:false,error:'FACTOR_SIMPLEX',row:i};if(!Array.isArray(r.reason_codes)||r.reason_codes.length<1||r.reason_codes.length>4||new Set(r.reason_codes).size!==r.reason_codes.length||r.reason_codes.some(x=>!REASONS.has(x)))return{ok:false,error:'REASON_CODES',row:i}}return{ok:true}}
async function assignmentContext(token){const {data:a,error:ae}=await supabase.from('asv2_assignments').select('assignment_id,cohort_id,work_id,status,expires_at,provider,participant_id,browser_hash,ip_hash,channel,claimed_at').eq('claim_token',token).single();if(ae||!a)throw new Error('UNKNOWN_CLAIM');const {data:w,error:we}=await supabase.from('asv2_work_items').select('work_id,anonymous_election_id,anonymous_territory_id,condition_id,batch_id').eq('work_id',a.work_id).single();if(we||!w)throw new Error('WORK_NOT_FOUND');const voterPath=`voter_batches/${w.anonymous_election_id}/${w.anonymous_territory_id}/${w.batch_id}.json`;const voterBatch=await readJson(voterPath);return{a,w,voterBatch,voterPath}}
async function buildSocialPreview(ctx,rows,modelLabel,r0Sha){const sourceKey=`voter_batches/${ctx.w.anonymous_election_id}/${ctx.w.anonymous_territory_id}/${ctx.w.batch_id}.json`;const s=await socializeContribution(rows,ctx.voterBatch.voter_archetypes,sourceKey);const payload={assignment_id:ctx.a.assignment_id,cohort_id:ctx.a.cohort_id,work_id:ctx.a.work_id,provider:ctx.a.provider||'other',model_label:modelLabel,r0_sha256:r0Sha,engine_version:'ATLAS_SOCIAL_LIVE_EDGE_V1',config_version:SOCIAL_CONFIG.schema_version,config_status:SOCIAL_CONFIG.status,graph_sha256:s.graph_sha256,profiles:s.profiles,graph:s.graph,summary:s.summary,scientific_status:'ACCEPTED'};const {error}=await supabase.from('asv2_social_previews').upsert(payload,{onConflict:'assignment_id'});if(error)throw error;return s.summary}
async function antiAbuse(rows,ctx,r0Sha,cfg){const partyKeys=Object.keys(rows[0].conditional_party_probabilities).sort();const canonical=rows.map(r=>({t:round6(r.turnout_probability),p:partyKeys.map(k=>round6(r.conditional_party_probabilities[k])),f:FACTORS.map(k=>round6(r.factor_importance[k])),rc:[...r.reason_codes].sort()}));const fingerprint=await sha256Json(canonical);const latency=Math.max(0,(Date.now()-new Date(ctx.a.claimed_at).getTime())/1000);const [{data:dupeFp},{data:dupeSha}]=await Promise.all([supabase.from('asv2_assignments').select('assignment_id,work_id').eq('behavior_fingerprint',fingerprint).neq('assignment_id',ctx.a.assignment_id).in('scientific_status',['ACCEPTED','QUARANTINED']).limit(1).maybeSingle(),supabase.from('asv2_assignments').select('assignment_id,work_id').eq('output_sha256',r0Sha).neq('assignment_id',ctx.a.assignment_id).in('scientific_status',['ACCEPTED','QUARANTINED']).limit(1).maybeSingle()]);let score=0;const flags=[];const flag=(code,points)=>{score+=points;flags.push({code,points})};if(dupeFp)flag('EXACT_BEHAVIOR_FINGERPRINT_REPEAT',100);if(dupeSha)flag('EXACT_OUTPUT_REPLAY',100);if(latency<3)flag('EXTREME_SUBMISSION_SPEED_METADATA_ONLY',10);const profile={schema_version:'ATLAS_ABUSE_PROFILE_V2',policy_version:POLICY_VERSION,content_based_exclusion:false,latency_seconds:latency,exact_behavior_repeat:!!dupeFp,exact_output_replay:!!dupeSha};return{fingerprint,profile,latency,score,flags,scientific_status:score>=Number(cfg.quarantine_score_threshold||100)?'QUARANTINED':'ACCEPTED'}}
function mapError(e){const m=String(e?.message||e||'SERVER_ERROR');if(m.includes('TURNSTILE_NOT_CONFIGURED'))return[503,'PROTECTION_NOT_CONFIGURED'];if(m.includes('TURNSTILE_'))return[403,m.match(/TURNSTILE_[A-Z_]+/)?.[0]||'TURNSTILE_FAILED'];if(m.includes('PARTICIPATION_TICKET')||m.includes('INVALID_PARTICIPATION_TICKET'))return[401,'PARTICIPATION_TICKET_INVALID'];if(m.includes('RATE_LIMIT')||m.includes('COOLDOWN_'))return[429,'RATE_LIMITED'];if(m.includes('NO_READY_WORK_AVAILABLE'))return[409,'NO_CONTRIBUTION_AVAILABLE'];if(m.includes('BROWSER_ID_INVALID'))return[400,'BROWSER_ID_INVALID'];return[500,'SERVER_ERROR']}
Deno.serve(async(req)=>{if(req.method==='OPTIONS')return new Response('ok',{headers:CORS});const url=new URL(req.url),path=url.pathname.replace(/\/+$/,'');try{if(req.method==='GET'&&(path.endsWith('/security-config')||path.endsWith('/config'))){const cfg=await securityConfig();return json({policy_version:POLICY_VERSION,turnstile_required:cfg.turnstile_required,turnstile_site_key:cfg.turnstile_site_key,protection_ready:cfg.turnstile_ready,ticket_ttl_seconds:cfg.ticket_ttl_seconds,limits:{active_per_participant:cfg.max_active_claims_per_participant,claims_per_participant_24h:cfg.max_claims_per_participant_24h,claims_per_ip_24h:cfg.max_claims_per_ip_24h}},200,'public, max-age=30, stale-while-revalidate=60')}if(req.method==='GET'&&path.endsWith('/status')){const [{data,error},cfg,readyStorage]=await Promise.all([supabase.rpc('asv2_public_collection_status'),securityConfig(),storageReady()]);if(error)throw error;const s=data?.[0]||null;return json({state:s?.state||'COLLECTING',completed:Number(s?.completed||0),target:Number(s?.target_work_items||2944),progress:Number(s?.target_work_items||0)?Number(s?.completed||0)/Number(s?.target_work_items):0,results_revealed:false,ready:!!(readyStorage&&cfg.turnstile_ready),protection_ready:cfg.turnstile_ready,policy_version:POLICY_VERSION},200,'public, max-age=15, stale-while-revalidate=45')}if(req.method==='POST'&&path.endsWith('/ticket')){const body=await req.json().catch(()=>({})),cfg=await securityConfig();const ps=await pseudonyms(req,body.browser_id,cfg);let outcome;try{outcome=await verifyTurnstile(req,body.turnstile_token,cfg)}catch(e){await supabase.from('asv2_security_events').insert({ip_hash:ps.ip_hash,browser_hash:ps.browser_hash,event_type:'TURNSTILE_REJECTED',detail:{policy_version:POLICY_VERSION}});throw e}const raw=randomToken(),ticketHash=await sha256Text(raw);const meta={success:true,hostname:outcome?.hostname||null,action:outcome?.action||null,challenge_ts:outcome?.challenge_ts||null,policy_version:POLICY_VERSION};const {data,error}=await supabase.rpc('asv2_issue_participation_ticket',{p_browser_hash:ps.browser_hash,p_ip_hash:ps.ip_hash,p_ticket_hash:ticketHash,p_turnstile_meta:meta});if(error)throw error;return json({participation_ticket:raw,expires_at:data?.[0]?.expires_at,policy_version:POLICY_VERSION})}if(req.method==='POST'&&path.endsWith('/claim')){if(!(await storageReady()))return json({error:'COLLECTION_NOT_READY'},409);const body=await req.json().catch(()=>({})),provider=String(body.provider||'other').toLowerCase();if(!['chatgpt','claude','other'].includes(provider))return json({error:'UNKNOWN_ASSISTANT'},400);const cfg=await securityConfig();const ps=await pseudonyms(req,body.browser_id,cfg);const rawTicket=String(body.participation_ticket||'');if(!rawTicket)return json({error:'PARTICIPATION_TICKET_REQUIRED'},401);const ticketHash=await sha256Text(rawTicket);const {data,error}=await supabase.rpc('asv2_claim_public_work_item_v2',{p_provider:provider,p_ticket_hash:ticketHash,p_browser_hash:ps.browser_hash,p_channel:'web'});if(error)throw error;const x=data?.[0];const {data:w,error:we}=await supabase.from('asv2_work_items').select('anonymous_election_id,anonymous_territory_id,condition_id,batch_id').eq('work_id',x.work_id).single();if(we)throw we;const voter=`voter_batches/${w.anonymous_election_id}/${w.anonymous_territory_id}/${w.batch_id}.json`,context=`contexts/${w.anonymous_election_id}/${w.condition_id}/${w.anonymous_territory_id}.json`;try{const[judge_prompt,output_schema,ctx,voter_batch]=await Promise.all([readText('as2_full_environment_prompt_v2.md'),readJson('as2_full_environment_output_schema_v2.json'),readJson(context),readJson(voter)]);const files={prompt:'data/contrib/prompt.txt',schema:'data/contrib/schema.json',context:`data/contrib/contexts/${w.anonymous_election_id}/${w.condition_id}/${w.anonymous_territory_id}.json.gz`,voter:`data/contrib/voter_batches/${w.anonymous_election_id}/${w.anonymous_territory_id}/${w.batch_id}.json.gz`};return json({assignment_id:x.assignment_id,claim_token:x.claim_token,expires_at:x.expires_at,work_id:x.work_id,files,payload:{judge_prompt,output_schema,context:ctx,voter_batch}})}catch(e){await supabase.from('asv2_assignments').update({status:'REJECTED',scientific_status:'QUARANTINED',abuse_score:100,abuse_flags:[{code:'PAYLOAD_LOAD_FAILURE',points:100}]}).eq('assignment_id',x.assignment_id);throw e}}if(req.method==='POST'&&path.endsWith('/submit')){const body=await req.json(),token=String(body.claim_token||'');if(!token)return json({error:'MISSING_CLAIM'},400);const ctx=await assignmentContext(token),check=validateRows(body.output_rows,ctx.w,ctx.voterBatch);if(!check.ok)return json({error:'INVALID_CONTRIBUTION',detail:check},422);const cfg=await securityConfig(),modelLabel=String(body.model_label||'UNSPECIFIED'),sha=await sha256Json(body.output_rows),abuse=await antiAbuse(body.output_rows,ctx,sha,cfg);const validation={ok:true,work_identity:true,row_order:true,schema_v2:true,anti_abuse_policy:POLICY_VERSION,content_based_exclusion:false};const {data,error}=await supabase.rpc('asv2_finalize_assignment_v2',{p_claim_token:token,p_model_label:modelLabel,p_output_rows:body.output_rows,p_output_sha256:sha,p_validation:validation,p_scientific_status:abuse.scientific_status,p_abuse_score:abuse.score,p_abuse_flags:abuse.flags,p_behavior_fingerprint:abuse.fingerprint,p_behavior_profile:abuse.profile,p_submission_latency_seconds:abuse.latency});if(error)throw error;if(abuse.scientific_status==='ACCEPTED'){try{await buildSocialPreview(ctx,body.output_rows,modelLabel,sha)}catch(e){console.error('social-preview',e)}}const d=data?.[0]||{};return json({status:'RECEIVED',social_status:'PROCESSING',cohort_id:d.cohort_id,work_id:d.work_id,completed_at:d.completed_at})}return json({name:'Société artificielle du Maroc',policy_version:POLICY_VERSION});}catch(e){console.error(e);const [status,code]=mapError(e);return json({error:code},status)}});
